name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Run CI test
        run: |
          echo "CI/CD pipeline đang chay..."
          echo "Xay dung thanh cong!"
